from fasthtml.common import *
# from strawberry.fastapi import GraphQLRouter
# from app.core.schemas.graphql.schema import schema

# Create GraphQL router (temporarily disabled - needs FastAPI compatibility)
router_graphql = APIRouter()

# TODO: GraphQL integration needs to be reworked for FastHTML
# For now, this is a placeholder router